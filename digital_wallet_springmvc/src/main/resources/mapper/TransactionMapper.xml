<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.digitalwallet.mapper.TransactionMapper">

    <resultMap id="txMap" type="com.digitalwallet.model.Transaction">
        <id property="id" column="id"/>
        <result property="fromWalletId" column="from_wallet_id"/>
        <result property="toWalletId" column="to_wallet_id"/>
        <result property="amount" column="amount"/>
        <result property="txType" column="tx_type"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO transactions(from_wallet_id, to_wallet_id, amount, tx_type, status)
        VALUES(#{fromWalletId}, #{toWalletId}, #{amount}, #{txType}, #{status})
    </insert>

    <select id="findByWalletId" resultMap="txMap">
        SELECT id, from_wallet_id, to_wallet_id, amount, tx_type, status, created_at
        FROM transactions
        WHERE from_wallet_id = #{walletId} OR to_wallet_id = #{walletId}
        ORDER BY created_at DESC
    </select>

</mapper>
